// you can write to stdout for debugging purposes, e.g.
// printf("this is a debug message\n");
#include <stdio.h>
#include <string.h> 
#include <stdlib.h> 

int solution(char *S) {
    // Implement your solution here
    // if(S == NULL){
    //     return 0; 
    // }
    
    // char * delimeter = "+, -"; 
    // char *token = strtok_r(S, delimeter, &temp); 
    // char * temp;
    // int operation = 0; 
    // int result; 
    // while(!token){
    //     if(strstr(token, "one") != 0){
    //         operation = 1; 
    //     } else if(strstr(token,"two") != 0){
    //         operation = 2; 
    //     }
    //     token = (NULL, delimeter, &temp); 
    // }

    // if(delimeter == '+'){
    //     operation 
    // }

    int operation; 
    int number = 0;
    int len = strlen(S); 
    for(int i=0; i<len; i++){
        if(S[i] == '\0'){
			if(strchr(S[i]))
		}
    } 
}
